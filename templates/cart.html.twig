{% extends "base1.html.twig" %}





{% block body %}
    <br> <br> <br> <br>
<div class="container-fluid">
    <div class="card">

        <table class="table table-hover shopping-cart-wrap">
            <thead class="text-muted">
            <tr>
                <th scope="col">Product</th>
                <th scope="col" width="120">Quantity</th>
                <th scope="col" width="120">Price</th>

                <th scope="col" width="120" class="text-right">Action</th>

            </tr>
            </thead>
            <tbody>

            {%  for product in products %}
            <tr>
                <td>
                    <figure class="media">
                        <div class="img-wrap"><img src="{{ asset(product.img) }}" class="img-thumbnail img-sm"></div>
                        <figcaption class="media-body">
                            <h6 class="title text-truncate">{{product.name}} </h6>

                        </figcaption>
                    </figure>
                </td>
                <td>
                    </var>{{product.quantity}}</var>
                </td>

                <td>
                    <div class="price-wrap">
                        <var class="price">{{product.quantity*product.price}}</var>
                        <small class="text-muted">{{ product.price}} each</small>
                    </div> <!-- price-wrap .// -->
                </td>


                <td class="text-right">
                    <a href=" /cart/remove/{{product.id}}" class="btn btn-outline-danger" > × Remove</a>
                </td> </tr><?php }}?>  <td>
                <div class="price-wrap">
                    <var class="price">Total price: $ {{ totalPrice }}</var>
            </div> </td>
        {% endfor %}

            </tbody>
        </table >

        <a class="btn btn-lg btn-primary text-uppercase" href="/place/order" >Place Order </a>

    </div> <!-- card.// -->
</div>
    </div>
{% endblock %}